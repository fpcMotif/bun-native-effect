## Agent skills

### Issue tracker

GitHub Issues for bug reports and Bun-fork implementation tasks; Linear for higher-level
project tracking. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`,
`wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the workspace root.
See `docs/agents/domain.md`.
